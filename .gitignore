# Claude Code local state / agent worktrees (ephemeral, not for the repo)
.claude/*

# ただしロール定義（6ロール・ユニット）はプロジェクトの一部として追跡する
!.claude/agents/

# Node deps (ephemeral test tooling like jsdom; not part of the app)
node_modules/
